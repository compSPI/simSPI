name: simSPI
channels:
  - conda-forge
  - defaults
dependencies:
  - flake8-black
  - numpy
  - pytest
  - pip
  - pytest-cov
  - pip:
    - git+https://github.com/compSPI/ioSPI.git
    - raster_geometry