name: simSPI
channels:
  - conda-forge
  - defaults
dependencies:
  - flake8-black
  - numba
  - numpy
  - pip
  - pip:
    - geomstats
    - git+https://github.com/geoffwoollard/ioSPI.git@move_functions_from_compspi
    - raster_geometry
  - pyfftw
  - pytest
  - pytest-cov
