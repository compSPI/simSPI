name: simSPI
channels:
  - conda-forge
  - defaults
dependencies:
  - flake8-black
  - numpy
  - pytest
  - pip
  - pytest-cov
  - pip:
    - git+https://github.com/geoffwoollard/compSPI/ioSPI.git@move_functions_from_compspi
    - raster_geometry